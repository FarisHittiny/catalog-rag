# Generation + judge go through one OpenAI-compatible endpoint (llm.py).
# The TAMU Chat API is OpenAI-compatible: https://chat-api.tamu.ai/openai
LLM_BASE_URL=https://chat-api.tamu.ai/openai
LLM_API_KEY=                    # bearer key; falls back to TAMU_CHAT_API_KEY if empty
TAMU_CHAT_API_KEY=
LLM_MODEL=                      # generator, e.g. protected.gpt-5.4-mini
JUDGE_MODEL=                    # judge; use a different model than LLM_MODEL

EMBED_MODEL=BAAI/bge-small-en-v1.5
RERANK_MODEL=BAAI/bge-reranker-base

# Demo API (api.py). Retrieval never needs a key; generate=true uses LLM_* above.
DEMO_ORIGINS=                   # comma-separated CORS allow-list, e.g. https://farishittiny.com
DEMO_GENERATE_PER_HOUR=20       # generated answers per client IP per hour
DEMO_DAILY_TOKENS=200000        # uncached generator tokens per UTC day
DEMO_PROXY_HOPS=0               # 1 behind one reverse proxy (the Dockerfile sets it for the Space)
